<% include ../partials/adminHeader.ejs  %>

<nav class="navbar fixed-top navbar-expand-lg navbar-dark fixed-top-1" style="background-color: rgb(13, 33, 67);">
        <div class = "container">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <a class="navbar-brand" href="/">
              Thrifty
          </a>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="#"><span class="sr-only">(current)</span></a>
                    </li>
                </ul>
                <ul class = "nav navbar-nav navbar-right">
                    <li class="nav-item">
                        <a class="nav-link" href="/adminview">All Requests</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/adminview1">Accepted Users</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/adminview2">Blocked Users</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="/balance">Load Balance</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logoutadmin">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <% if(error && error.length > 0) { %>
          <div class = "alert alert-danger" role = "alert">
            <%= error %>
          </div>
          <% } %>
        <% if(success && success.length > 0) { %>
          <div class = "alert alert-success" role = "alert">
            <%= success %>
          </div>
          <% } %>
      </div>

<div class="container mt-4">
        <a href="/adminlogged" class = "btn btn-lg btn-outline-primary mb-3"><- Admin Main Page</a>

        <% newUser.forEach(function(newUser){ %>
        <% if(newUser.status === true && newUser.blocked === false){ %>
                <% if(newUser.admin_status === true){ %>
                <% }else{ %>
            <div class="card w-75 mb-3">
                    <div class="card-body">
                        <h5 class="card-title"><%= newUser.first_name %> <%= newUser.last_name %></h5>
                        <p class="card-text"><%= newUser.address %></p>
                        <p class="card-text"><%= newUser.email %></p>
                        <div class = "text-right">
                            <a href="/balance/<%= newUser._id %>" class="btn btn-primary float-left">More Info</a>
                            
                            <a href="/balance/<%= newUser._id %>/load" class="btn btn-success float-right">Load Balance</a>
                            <!-- <button id = "myBtn"class="btn btn-success">Accept</button> -->
                        </div>
                    </div>
                </div>
        <% } %>
        <% } %>
        <% }) %>
</div>

<% include ../partials/footer.ejs  %>

