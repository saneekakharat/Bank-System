<% include ../partials/adminHeader.ejs  %>
<nav class="navbar fixed-top navbar-expand-lg navbar-dark fixed-top-1" style="background-color: rgb(13, 33, 67);">
    <div class = "container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="/">
          Thrifty
      </a>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="#"><span class="sr-only">(current)</span></a>
                </li>
            </ul>

        </div>
    </div>
</nav>

<div class="container">
    <% if(error && error.length > 0) { %>
      <div class = "alert alert-danger" role = "alert">
        <%= error %>
      </div>
      <% } %>
    <% if(success && success.length > 0) { %>
      <div class = "alert alert-success" role = "alert">
        <%= success %>
      </div>
      <% } %>
  </div>

<!-- <div class="row"> -->
<div class="container">
<a href="/balance" class = "btn btn-lg btn-outline-primary"><- View all users</a>

        <h1 class = "display-4 mb-3" style="text-align: center; color: white;">Load Balance</h1>
    <form action="/balance/<%= viewUser._id %>?_method=PUT" method="POST">
        <div class = "form-group">
            <input class = "form-control" type="number" placeholder = "amount in ₹" name= "account_balance" min = "0">
        </div>
        <div class = "form-group">
            <button class = "btn btn-lg btn-default btn-primary btn-block">Load Balance</button>
        </div>
        <p class="card-text"><small class="text-muted">Enter the amount only after checking the recieved money.</small></p>
    </form>
</div>

<% include ../partials/footer.ejs  %>